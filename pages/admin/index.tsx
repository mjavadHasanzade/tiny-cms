import Layout from "@organisms/layout";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <Layout translations={""} isLogin={true}>
      Home Page
    </Layout>
  );
};

export default Home;
